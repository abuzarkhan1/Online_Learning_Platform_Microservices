spring:
  datasource:
    url: jdbc:mysql://localhost:3306/enrollmentdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 3003

enrollment:
  jwt:
    secret: JWT_SECRETJWT_SECRETJWT_SECRETJWT_SECRETJWT_SECRET
